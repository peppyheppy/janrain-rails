# this file was originally generated by the janrain-rails generator
defaults: &default
  capture:
    model: '<%= class_name %>'
    controller: '<%= controller_name.downcase %>'
    client_id: ''
    client_secret: ''
    domain: 'https://myapp.dev.janraincapture.com'
    redirect_url: 'www.myapp.dev/<%= controller_name %>/new'
    within_iframe: true
    entity:
      # see the rails_setup migration for field information
      ignore_columns: [ ]

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default

